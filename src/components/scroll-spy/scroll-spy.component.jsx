import React, { useEffect } from "react";

// const routes = ["profile", "experience", "projects", "education"];

const ScrollSpy = ({ entries }) => {
  // const [currentSection, setCurrentSection] = useState();

  useEffect(() => {
    
  }, [entries]);
  // console.log(entries);
  return <div></div>;
};

export default ScrollSpy;
